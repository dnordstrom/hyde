#!/usr/bin/env ruby -w

require "rack"
require "hyde"

app = Rack::Builder.new do
  use Rack::ShowExceptions
  run Hyde::Application.new
end

app = Rack::Session::Pool.new(
  Hyde::Application.new,
  expire_after: 3600
)

Rack::Server.start(app: app, Port: 5000)
